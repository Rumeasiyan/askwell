/target
# tauri-build writes these — the autogenerated allow-/deny- permission TOMLs
# for `M7-TAURI-FE-182`'s five commands, and the schema/capabilities JSON it
# derives from them. Generated at build time from `build.rs`'s `AppManifest`
# call and `capabilities/default.json`; never hand-edited, never committed.
/permissions/autogenerated
/gen
