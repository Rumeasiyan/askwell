import { AddScreen } from "@/components/add/add-screen";

/**
 * `/sources/add/` — `docs/ux/add-source.md`.
 *
 * A drop anywhere in the application lands here, so this route is reached far
 * more often than it is navigated to.
 */
export default function AddSourcePage() {
  return <AddScreen />;
}
