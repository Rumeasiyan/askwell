# Exact versions, always. The four dependencies in this project are a verified
# set, not four independent ranges — `pnpm add` writing a caret would quietly
# reintroduce the problem this ticket exists to solve.
save-exact=true
save-prefix=

